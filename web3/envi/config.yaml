# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: envi
networks:
- id: 11155111
  start_block: 0
  contracts:
  - name: challengercc
    address:
    - 0xe38d8f585936c60ecb7bfae7297457f6a35058bb
    handler: ./src/EventHandlers.js
    events:
    - event: ChallengeCreated(uint256 indexed challengeId, address indexed creator, string description, uint256 stakeAmount, uint256 startTime, uint256 endTime, uint256 targetDistance)
    - event: ChallengeFinalized(uint256 indexed challengeId, uint256 totalWinners, uint256 totalLosers)
    - event: TaskCompleted(uint256 indexed challengeId, address indexed user, uint256 completionTimestamp, uint256 distance, uint256 duration, string stravaActivityId)
    - event: UserJoined(uint256 indexed challengeId, address indexed user, uint256 stakedAmount)
    - event: WinningsDistributed(uint256 indexed challengeId, address indexed winner, uint256 amount)
unordered_multichain_mode: true
preload_handlers: true
